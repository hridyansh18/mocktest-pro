import {Bell,Menu} from 'lucide-react';import {useAuth} from '../context/AuthContext';export default function Topbar(){const {admin}=useAuth();return <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-white/10 bg-[#080d1c]/75 px-4 backdrop-blur-xl md:px-8"><div className="flex items-center gap-3"><button onClick={()=>window.dispatchEvent(new Event('mtp:menu'))} className="glass grid h-10 w-10 place-items-center rounded-xl lg:hidden"><Menu size={19}/></button><div><p className="text-xs text-slate-500">ADMIN PORTAL</p><h2 className="font-semibold">Welcome back, {admin?.fullName?.split(' ')[0]||'Admin'} 👋</h2></div></div><div className="flex items-center gap-3"><button className="glass grid h-10 w-10 place-items-center rounded-xl"><Bell size={18}/></button><div className="hidden text-right sm:block"><p className="text-sm font-semibold">{admin?.fullName}</p><p className="text-xs text-slate-500">{admin?.institution||'Administrator'}</p></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-gradient-to-br from-blue-500 to-violet-600 font-bold">{admin?.fullName?.[0]||'A'}</div></div></header>}