import {useEffect,useState} from 'react';import {Trophy} from 'lucide-react';import api from '../../services/api';export default function Leaderboard(){const [tests,setTests]=useState([]),[testId,setTestId]=useState(''),[data,setData]=useState({enabled:true,entries:[]});useEffect(()=>{api.get('/admin/tests?limit=100').then(r=>{const x=r.data.data.tests||[];setTests(x);if(x[0])setTestId(x[0].id)})},[]);useEffect(()=>{if(testId)api.get(`/admin/tests/${testId}/leaderboard`).then(r=>setData(r.data.data))},[testId]);return <div><h1 className="text-3xl font-bold">Leaderboard</h1><p className="mt-2 text-sm text-slate-400">Ranked by score and completion time.</p><select className="field mt-6 max-w-xl" value={testId} onChange={e=>setTestId(e.target.value)}>{tests.map(t=><option key={t.id} value={t.id}>{t.title} — {t.test_code_id}</option>)}</select>{!data.enabled&&<p className="mt-5 rounded-xl bg-amber-500/10 p-4 text-amber-400">Leaderboard is disabled for this test.</p>}<div className="mt-5 space-y-3">{data.entries?.map(r=><div key={`${r.enrollment_number}-${r.rank}`} className="glass flex items-center gap-4 rounded-2xl p-5"><div className="grid h-11 w-11 place-items-center rounded-xl bg-violet-500/10 text-xl font-black text-violet-400">#{r.rank}</div><div className="flex-1"><p className="font-semibold">{r.full_name}</p><p className="text-xs text-slate-500">{r.enrollment_number}</p></div><div className="text-right"><p className="font-bold">{r.score} marks</p><p className="text-xs text-slate-500">{Number(r.percentage||0).toFixed(1)}% · {Math.floor(Number(r.time_taken_seconds||0)/60)}m {Number(r.time_taken_seconds||0)%60}s</p></div></div>)}{!data.entries?.length&&<div className="glass rounded-2xl py-16 text-center text-slate-500"><Trophy className="mx-auto mb-3"/><p>No ranked submissions yet.</p></div>}</div></div>}