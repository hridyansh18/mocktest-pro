import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ShieldCheck,LoaderCircle} from 'lucide-react';import {useAuth} from '../../context/AuthContext';
export default function Login(){const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(form);nav('/admin')}catch(err){setError(err.response?.data?.message||'Unable to sign in')}finally{setBusy(false)}};return <div className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7 md:p-9"><div className="mb-7"><div className="mb-5 grid h-12 w-12 place-items-center rounded-2xl bg-gradient-to-br from-blue-500 to-violet-600"><ShieldCheck/></div><h1 className="text-2xl font-bold">Admin sign in</h1><p className="mt-2 text-sm text-slate-400">Secure access to MockTest Pro</p></div>{error&&<p className="mb-4 rounded-xl bg-rose-500/10 p-3 text-sm text-rose-400">{error}</p>}<div className="space-y-4"><input className="field" type="email" placeholder="Email address" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input className="field" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button disabled={busy} className="btn-primary flex w-full justify-center">{busy?<LoaderCircle className="animate-spin"/>:'Sign in'}</button></div><p className="mt-6 text-center text-sm text-slate-500">New administrator? <Link className="text-blue-400" to="/register">Create account</Link></p><p className="mt-8 text-center text-xs text-slate-600">Designed & Developed by <span className="text-violet-400">Hridyansh Chaudhary</span></p></form></div>}