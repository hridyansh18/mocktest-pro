import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../../context/AuthContext';export default function Register(){const [f,setF]=useState({fullName:'',email:'',password:'',institution:'',designation:''});const [error,setError]=useState('');const {register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();try{await register(f);nav('/admin')}catch(err){setError(err.response?.data?.message||'Registration failed')}};return <div className="grid min-h-screen place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-lg rounded-3xl p-8"><h1 className="text-2xl font-bold">Create admin account</h1><p className="mb-6 mt-2 text-sm text-slate-400">Set up your examination workspace</p>{error&&<p className="mb-4 text-sm text-rose-400">{error}</p>}<div className="grid gap-4 sm:grid-cols-2">{Object.entries(f).map(([k,v])=><input key={k} className={`field ${k==='email'||k==='password'?'sm:col-span-2':''}`} type={k==='password'?'password':k==='email'?'email':'text'} placeholder={{fullName:'Full name',email:'Email address',password:'Password (8+ characters)',institution:'Institution',designation:'Designation'}[k]} value={v} onChange={e=>setF({...f,[k]:e.target.value})} required={['fullName','email','password'].includes(k)}/>)}</div><button className="btn-primary mt-5 w-full">Create account</button><p className="mt-5 text-center text-sm text-slate-500">Already registered? <Link to="/login" className="text-blue-400">Sign in</Link></p></form></div>}