import axios from 'axios';
const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://localhost:5000/api'});
api.interceptors.request.use(c=>{const t=localStorage.getItem('mtp_access');if(t)c.headers.Authorization=`Bearer ${t}`;return c});
api.interceptors.response.use(r=>r,async err=>{const original=err.config;if(err.response?.status===401&&!original?._retry&&localStorage.getItem('mtp_refresh')){original._retry=true;try{const {data}=await axios.post(`${api.defaults.baseURL}/auth/refresh`,{refreshToken:localStorage.getItem('mtp_refresh')});localStorage.setItem('mtp_access',data.data.accessToken);if(data.data.refreshToken)localStorage.setItem('mtp_refresh',data.data.refreshToken);original.headers.Authorization=`Bearer ${data.data.accessToken}`;return api(original)}catch{localStorage.removeItem('mtp_access');localStorage.removeItem('mtp_refresh');localStorage.removeItem('mtp_admin');window.location.href='/login'}}return Promise.reject(err)});
export default api;